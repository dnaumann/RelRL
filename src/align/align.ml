open Astutil

(* let align prog1 prog2 set_output =  *)


    (*  
let run penv ctbl left_module left_method right_module right_method set_output =
  if progs <> 1 
    then   
    else  
      let prog = concat_map parse_file files in
      
      Printf.fprintf stderr "Finished Aligning\n"

      parse_program;
      check two unary programs if not "Error! Expected two unary programs"
      create_default_bicom;
      prepare_egraph with default bicom;
      saturate;
      pick with llm;
      print bicom;
      verify bicom;
      repeat to picking. *)
